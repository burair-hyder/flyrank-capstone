import PagePlaceholder from "@/components/ui/PagePlaceholder";

export default function AnalyticsPage() {
  return (
    <PagePlaceholder
      title="Analytics"
      description="Review completion rates, overdue trends, category distribution, and priority statistics."
    />
  );
}