import PagePlaceholder from "@/components/ui/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="View task statistics, recent activity, upcoming deadlines, overdue work, and completion progress."
    />
  );
}