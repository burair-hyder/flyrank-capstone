import PagePlaceholder from "@/components/ui/PagePlaceholder";

export default function NewTaskPage() {
  return (
    <PagePlaceholder
      title="Create a Task"
      description="Add a task with a title, description, status, priority, category, and due date."
    />
  );
}