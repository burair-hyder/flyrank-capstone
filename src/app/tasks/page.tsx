import PagePlaceholder from "@/components/ui/PagePlaceholder";

export default function TasksPage() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Search, filter, sort, edit, complete, and remove tasks from one centralized task list."
    />
  );
}