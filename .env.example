# Variables beginning with NEXT_PUBLIC_ may be exposed to the browser.
NEXT_PUBLIC_APP_NAME=TaskFlow
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only variables must not begin with NEXT_PUBLIC_.
API_SECRET=
DATABASE_URL=